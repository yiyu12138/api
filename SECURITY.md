# 安全说明

API Balance 没有内置账号登录。不要把端口直接暴露到公网；请在反向代理、VPN、防火墙或 NAS 权限层完成访问控制和 HTTPS。

- Docker 使用 `APP_SECRET` 加密站点与推送凭据，首次使用后不要更换；飞牛和 Android 使用各自的应用私有密钥。
- `APP_SECRET`、`.env`、平台数据目录和导出配置均按敏感资料保管。
- 安装编号和许可证保存在平台数据中。覆盖升级会保留，卸载并删除数据或重装设备后通常会变化；Android 配置导出不包含安装编号和许可证。
- 自定义计费、用量和 Webhook URL 会由后端主动访问，只填写可信地址。
- 提交问题时用占位密钥替代真实 API Key，并删除响应中的账号信息。

安全问题请通过 `avhlune@gmail.com` 私下反馈，不要公开真实凭据或漏洞细节。
